# Agent 飞行记录仪：个人项目验收手册

> 文档版本：v0.1  
> 文档日期：2026-09-03  
> 适用对象：不以代码审查为主要手段的 AI Native 开发者  
> 关联文档：[产品定义手册](./Agent飞行记录仪-产品定义手册.md) · [MVP 开发文档](./Agent飞行记录仪-开发文档.md)

---

## 1. 你真正要验收什么

你不需要通过阅读代码判断项目是否完成。你需要确认五件可观察的事情：

1. **看得见**：Agent 做过的关键动作都出现在时间线中。
2. **拦得住**：危险动作在真正执行前暂停，必须由你决定。
3. **对得上**：页面展示的命令、文件和结果与磁盘上的真实变化一致。
4. **回得去**：失败后能在隔离环境中重新运行，不破坏原项目。
5. **说得清**：任何没记录到的内容都会明确提示，不会伪装成“完整记录”。

验收的核心不是“页面做完了多少”，而是这五件事能否稳定、重复地发生。

## 2. 最终通过标准

项目满足以下条件才算通过个人展示版验收：

- 三个核心 Demo 都能连续成功运行两次；
- 第 6 节的 P0 检查项全部通过；
- 第 8 节的红线问题为零；
- 从全新下载项目到看到第一条 Run，不超过 10 分钟；
- 另一位不了解代码的人只看 README，也能跑通至少一个 Demo；
- 整个演示过程不需要临时改代码、改数据库或手工伪造记录。

如果某个“应该记录的动作”没有被采集，只要系统明确显示了采集缺口，可以记为限制；如果系统悄悄漏掉却仍声称记录完整，则验收失败。

## 3. 验收前应准备的内容

### 3.1 项目交付物

在开始验收前，项目目录中至少应有：

- `README.md`：安装、启动和停止方式；
- `.env.example`：示例配置，不能包含真实密钥；
- 一个固定的 `demo-project`；
- 三个 Demo 的任务文本；
- 一条启动命令和一条测试命令；
- 已知限制清单；
- 当前版本号和支持的 macOS、Agent 版本。

如果缺少这些材料，即使程序能在开发电脑上运行，也不适合作为可展示成果验收。

### 3.2 验收环境

建议使用：

- 一台 macOS 电脑；
- 一个新终端窗口；
- 一个全新复制的 Demo 项目；
- 专门用于演示的假 Token；
- 屏幕录制工具；
- 浏览器无痕窗口，避免旧缓存影响结果。

验收中绝对不要使用真实 API 密钥、生产账号、重要代码仓库或真实外部发送目标。

### 3.3 两种 Agent 模式

为了兼顾稳定验收和真实展示，建议保留两种入口：

| 模式 | 用途 | 验收要求 |
|---|---|---|
| 固定示例 Agent | 自动执行确定的命令和文件动作 | 用于重复验收，结果必须稳定 |
| 真实 Codex 接入 | 展示真实 Agent 使用体验 | 用于作品演示，允许执行路径略有变化 |

固定示例 Agent 不是造假。它相当于自动化测试夹具，用来确认 AFR 本身稳定；真实 Codex 用来证明产品能接入实际场景。

## 4. 先做 10 分钟快速验收

如果以下任一项失败，先停止完整验收并修复：

1. 根据 README 启动项目，不修改源代码。
2. 浏览器能打开 Runs 首页，页面没有持续报错。
3. 运行官方 Demo 任务，10 秒内出现一条新的 Run。
4. Run 详情至少出现一条命令事件和一条文件事件。
5. 修改后的文件能显示 diff。
6. 停止并重新启动 AFR，刚才的 Run 仍然存在。
7. 页面能明确显示当前版本和支持范围。

快速验收通过后，再执行下面的完整场景。

## 5. 三个核心验收 Demo

### 5.1 Demo A：拦截危险删除

#### 准备

在 Demo 项目中提供一个受保护文件，例如：

```text
demo-project/protected/important.txt
```

文件内容使用无价值的测试文本。策略将 `protected/` 配置为受保护路径。

#### 操作

1. 启动 AFR 和 Demo Agent。
2. 让 Agent 删除 `protected/important.txt`。
3. 看到审批卡片后，先不要点击任何按钮。
4. 在 Finder 或编辑器中确认文件仍然存在。
5. 检查审批卡片是否显示 Actor、动作、完整路径、风险等级、理由和可恢复性。
6. 点击“拒绝”，再次确认文件仍然存在。
7. 重新发起同一任务，这次点击“仅本次批准”。
8. 确认执行动作与审批卡片中展示的目标完全一致。
9. 查看 Run 时间线中的策略判断、审批决定和最终执行结果。

#### 通过标准

- 审批前没有删除文件；
- 拒绝后没有删除文件；
- 批准后只影响审批中显示的目标；
- 审批者、时间、动作参数和决定均被记录；
- 文件删除前存在可查看的快照或恢复依据。

#### 立即失败条件

- 页面出现审批卡片时，文件已经被删除；
- 审批显示一个路径，实际删除另一个路径；
- 拒绝后 Agent 通过换一种命令完成了同一删除；
- Agent 可以自行写入“用户已批准”。

### 5.2 Demo B：看懂一次代码修改

#### 准备

Demo 项目应包含一个简单 Bug 和对应测试。例如：除数为零时没有返回明确错误。任务文本固定为：

> 修复除数为零时的处理，运行相关测试，并说明修改了什么。

#### 操作

1. 从 Runs 首页开始录屏。
2. 启动任务并等待完成。
3. 打开 Run 详情，不查看终端原始输出，先尝试只通过页面回答：
   - Agent 接到的任务是什么？
   - 它读取了哪些关键文件？
   - 它运行了哪些命令？
   - 它具体修改了哪几行？
   - 测试是否通过？
4. 点击文件事件，查看变更前后 diff。
5. 点击测试命令，查看退出码和输出摘要。
6. 打开真实文件，确认页面中的 diff 与磁盘内容一致。
7. 检查 Run 最终状态是否与真实测试结果一致。

#### 通过标准

- 一分钟内可以理解这次 Run 的主要过程；
- 命令、文件路径、diff、退出码和时间顺序正确；
- 页面显示的测试结果与真实结果一致；
- 失败、重试或未采集事件不会被显示成成功；
- 大段输出默认折叠，但可以按需查看。

#### 立即失败条件

- Agent 实际改了文件，时间线却没有文件事件，也没有采集缺口提示；
- 测试失败，但 Run 显示“已成功验证”；
- diff 与磁盘真实内容不一致；
- 页面只有 Agent 的自然语言总结，没有底层命令或文件证据。

### 5.3 Demo C：失败后创建隔离分支

#### 准备

Demo 项目提供一个会稳定失败的任务路径，例如让固定示例 Agent 先安装错误依赖或写入错误配置。项目根目录放置：

```text
ORIGINAL_MARKER.txt
```

它用于确认回放没有修改原工作区。

#### 操作

1. 运行会失败的任务。
2. 在 Run 时间线中找到首个失败步骤。
3. 选择失败前的 Checkpoint，点击“创建分支并回放”。
4. 在回放设置中修改提示词或依赖约束。
5. 启动回放，确认页面显示新的 Run ID 和隔离 worktree 路径。
6. 等待新分支完成，查看新旧 Run 对比。
7. 检查原项目中的 `ORIGINAL_MARKER.txt` 和其他文件是否保持不变。
8. 检查所有新文件和修改是否只存在于 worktree。

#### 通过标准

- 新 Run 明确关联原 Run 和分叉点；
- 回放发生在独立 worktree；
- 原工作目录没有被回放修改；
- 新旧状态、命令、文件和测试差异可见；
- 回放中遇到外部副作用时默认阻止或模拟。

#### 立即失败条件

- 回放直接修改原项目；
- 新 Run 无法说明从哪里分叉；
- 外部发送、删除或发布动作在回放中自动真实执行；
- 页面显示回放成功，但 worktree 中的测试实际失败。

## 6. P0 验收清单

每项只能标记为“通过”“失败”或“不适用”。不要使用“基本通过”。

### 6.1 安装与启动

- [ ] 根据 README 可在 10 分钟内启动。
- [ ] 不需要修改源代码、数据库或隐藏配置。
- [ ] 启动失败时能看到可理解的错误提示。
- [ ] 页面显示版本、数据目录和支持范围。

### 6.2 运行记录

- [ ] 新任务能自动创建 Run。
- [ ] Run 状态与任务真实状态一致。
- [ ] 命令包含参数、工作目录、退出码和耗时。
- [ ] 文件事件包含路径、动作、哈希和文本 diff。
- [ ] 事件按真实发生顺序展示。
- [ ] 未采集内容有明显缺口提示。

### 6.3 审批与安全

- [ ] 受保护文件删除在执行前暂停。
- [ ] 拒绝后目标没有变化。
- [ ] 批准内容与实际执行内容完全一致。
- [ ] 已使用的单次批准不能再次使用。
- [ ] Agent 不能修改保护规则或伪造审批。
- [ ] 路径超出 Demo 项目时默认拒绝。

### 6.4 快照与回放

- [ ] 高风险文件动作前创建快照。
- [ ] 可以从失败前检查点创建新 Run。
- [ ] 回放目录与原工作目录明显不同。
- [ ] 原工作目录在回放前后保持不变。
- [ ] 新旧 Run 可以查看关键差异。

### 6.5 持久化与隐私

- [ ] 重启 AFR 后历史 Run 仍然存在。
- [ ] 未完成 Run 不会被错误显示为成功。
- [ ] 假 Token 在页面、导出文件和日志中均被遮盖。
- [ ] Run JSON 能成功导出并正常打开。

### 6.6 展示体验

- [ ] 一分钟内可以找到一次 Run 的关键修改。
- [ ] 三个 Demo 各自可以在 3～5 分钟内讲清楚。
- [ ] 页面没有必须由作者口头解释才能理解的关键状态。
- [ ] 空数据、加载、失败和等待审批状态都有明确反馈。

## 7. AI Native 验收方式

你可以让 AI 帮助执行机械步骤，但不要让 AI 替你判断最终是否通过。

推荐流程：

1. 让 AI 根据 README 启动项目，并逐条报告它执行的命令。
2. 你亲自观察 AFR 是否完整记录了这些命令。
3. 让 AI 执行固定 Demo，不允许它临时修改测试夹具来“让测试通过”。
4. 让 AI 输出失败证据，包括截图位置、Run ID 和复现步骤。
5. 你根据“真实文件是否变化”和“页面记录是否一致”作最终判断。

可以直接给 AI 以下提示词：

> 请严格按照《Agent 飞行记录仪：个人项目验收手册》执行 Demo A/B/C。不要修改验收标准，不要为了通过而更改测试夹具。每一步记录 Run ID、操作、实际结果和对应证据；遇到失败立即保留现场并给出复现步骤。

不要只让 AI 回答“项目已经完成”。AFR 本身就是为了验证 Agent 的行为，所以验收也必须依赖可检查的操作记录。

## 8. 一票否决的红线

出现以下任意问题，当前版本不能标记为展示版完成：

1. 未审批的危险动作真实执行。
2. 审批后实际动作与展示参数不一致。
3. 回放修改原工作目录。
4. 系统把失败显示为成功。
5. 系统漏记关键动作，却没有显示采集缺口。
6. 假 Token 未经遮盖出现在 UI、数据库导出或日志中。
7. 历史审批可以被 Agent 删除或改写。
8. 三个 Demo 依赖临时改代码、手工改数据库或预先伪造数据。

## 9. 问题分级

| 等级 | 定义 | 处理方式 |
|---|---|---|
| S0 红线 | 造成未授权副作用、数据泄露、记录造假或原工作区被破坏 | 立即停止展示，修复后完整重验 |
| S1 阻塞 | 核心 Demo 无法完成、记录与真实状态不一致、无法启动或恢复 | 修复后重验相关 Demo 和回归项 |
| S2 重要 | 流程可完成，但信息缺失、操作困难或容易误解 | 公开展示前修复 |
| S3 优化 | 文案、视觉、加载速度或非关键交互问题 | 记录到后续清单，不阻塞展示 |

个人项目不需要复杂缺陷系统。使用一个 Markdown 表格记录编号、版本、复现步骤、期望结果、实际结果、等级和修复状态即可。

## 10. 验收记录模板

```markdown
# AFR 展示版验收记录

- 版本：
- 日期：
- macOS 版本：
- Agent 及版本：
- Git commit：
- 数据目录：

## 快速验收

- 结果：通过 / 失败
- 首次启动耗时：
- 第一条 Run 出现耗时：

## Demo A：危险删除

- 第一次 Run ID：
- 第二次 Run ID：
- 结果：通过 / 失败
- 证据：
- 问题：

## Demo B：代码修改记录

- 第一次 Run ID：
- 第二次 Run ID：
- 结果：通过 / 失败
- 证据：
- 问题：

## Demo C：失败后分叉

- 原 Run ID：
- 分叉 Run ID：
- 原目录回放前后哈希是否一致：
- 结果：通过 / 失败
- 证据：
- 问题：

## P0 检查表

- 通过：__/__
- 失败项：
- 不适用项及原因：

## 红线检查

- S0 数量：
- S1 数量：

## 最终结论

- [ ] 通过，可以公开展示
- [ ] 有条件通过，修复 S2 后展示
- [ ] 不通过，修复 S0/S1 后重新验收
```

## 11. 最实用的验收顺序

不要在项目全部写完后才第一次验收。按开发阶段逐步检查：

1. **第 1～3 周**：只验收 Demo B，确认记录可信。
2. **第 4～5 周**：只验收 Demo A，确认危险操作真的被拦截。
3. **第 6～7 周**：只验收 Demo C，确认回放不碰原目录。
4. **第 8～9 周**：从全新环境完整跑两轮，整理截图和演示视频。

只要 Demo B 的记录还不可信，就不要继续做审批；只要审批不能真正阻止动作，就不要继续做回放。这样能避免个人项目在错误基础上不断堆功能。
