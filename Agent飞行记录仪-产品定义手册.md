# Agent 飞行记录仪：产品定义手册

> 暂定英文名：Agent Flight Recorder（AFR）  
> 文档版本：v0.1  
> 文档日期：2026-09-03  
> 产品阶段：概念定义 / MVP 规划  
> 默认部署形态：本地优先的完整程序，提供 SDK、Hook、网关和 MCP 接入方式

---

## 1. 文档目的

本文用于统一 Agent 飞行记录仪的产品定位、目标用户、核心问题、功能边界、交互流程、技术形态、MVP 范围和成功标准，可作为后续原型设计、技术方案、开发排期和用户访谈的共同基线。

本文中的“Agent”主要指能够调用终端、文件系统、浏览器、数据库、MCP 工具或第三方服务，并可连续执行多步任务的 AI Agent，包括但不限于编码 Agent、办公 Agent、研究 Agent和企业内部自动化 Agent。

## 2. 一句话定义

Agent 飞行记录仪是一套面向 AI Agent 的可观测、审批、取证与回放平台：它持续记录 Agent 做过什么、为什么这样做、修改了什么、依据是什么，并允许用户在高风险操作前审批，在失败后定位问题，从任意步骤安全重放或分叉运行。

一个更直观的比喻是：

> Agent 版的 Sentry + Git + 行车记录仪 + 权限控制台。

## 3. 产品背景

### 3.1 Agent 正从“回答问题”变成“操作世界”

传统聊天模型主要生成文本，错误通常停留在回答层。新一代 Agent 会读取项目文件、运行命令、修改代码、浏览网页、发送消息、操作云端数据，甚至调用支付、部署和生产系统。

当 AI 获得工具调用能力后，风险性质发生了变化：错误不再只是“答错”，而可能成为真实且难以恢复的外部操作。

### 3.2 Agent 的执行过程越来越长、越来越不透明

一个复杂任务可能包含几十到数百个步骤，并穿过模型、工具、文件、网页和人工审批环节。现有聊天记录通常只展示部分推理摘要和零散的工具输出，无法可靠回答：

- Agent 在第几步开始偏离目标？
- 它依据了哪份文件或网页？
- 某次文件修改由哪个指令触发？
- 同一任务为什么昨天成功、今天失败？
- 换一个模型、提示词或工具版本会怎样？
- 最终结果是否经过测试或证据验证？

### 3.3 社区信号

近期开发者社区的讨论已经从“Agent 能不能做事”转向“如何约束、验证和恢复 Agent”：

- Agent 持久记忆、上下文管理和经验积累成为高频议题；
- Agent 误删数据、权限过大和沙箱逃逸风险持续受到关注；
- 可验证执行、运行回放、人工审批和证据链开始出现独立项目；
- 多 Agent 协作让错误来源和责任归属更加复杂；
- MCP 降低了工具接入成本，也扩大了 Agent 可操作面的范围。

这些信号说明，Agent 的下一阶段基础设施不会只有“更多工具”，还需要一套可信的运行记录和控制平面。

## 4. 要解决的核心痛点

### 4.1 黑箱执行：用户不知道 Agent 实际做了什么

现状：用户通常只能看到自然语言总结，无法快速检查完整的工具调用、输入参数、返回结果、文件变化和外部副作用。

后果：

- 出现事故时难以复盘；
- 用户不敢把更重要的任务交给 Agent；
- 团队无法形成可审核、可追责的执行记录；
- Agent 声称“已完成”时，用户缺乏低成本验收方式。

### 4.2 高风险操作缺少统一的审批闸门

现状：终端、浏览器、MCP 和第三方 API 各自拥有不同的授权方式。审批规则分散在不同客户端中，难以统一管理。

典型风险包括：

- 删除或覆盖文件；
- 推送代码、部署或修改生产环境；
- 发送邮件、消息或公开内容；
- 修改云端权限；
- 发起付款、购买或订阅；
- 导出、上传或传输敏感数据；
- 执行来源不明的脚本。

### 4.3 失败难定位，也难稳定复现

现状：Agent 任务失败后，重新运行往往会得到不同路径。模型、上下文、工具返回和外部状态的变化都会影响结果。

后果：

- 开发者无法像调试普通程序一样复现故障；
- 提示词和模型调整只能凭感觉；
- Agent 框架团队难以建立回归测试；
- 线上问题需要人工阅读大量日志。

### 4.4 结果与证据脱节

现状：Agent 可以生成看似完整的报告或代码，但没有把结论、引用、测试、文件修改和工具输出组织成可核验的证据链。

后果：

- “完成”与“正确”被混为一谈；
- 引用可能失效或不能支持结论；
- 代码可能通过生成但没有经过测试；
- 团队审核只能重新执行整项工作。

### 4.5 长任务缺少连续、可管理的状态

现状：任务可能跨越数小时或数天，期间需要等待人工回复、外部系统或计划任务。普通会话记录不能可靠表达任务状态、未决事项和恢复点。

后果：Agent 容易忘记前置条件、重复操作，或在恢复后使用过期上下文。

### 4.6 多 Agent 协作缺乏责任边界

现状：多个 Agent 并行工作时，经常共享文件、工具和任务目标。一旦结果出错，很难判断是哪一个 Agent、哪次交接或哪项工具调用造成的。

## 5. 产品愿景与目标

### 5.1 愿景

让每一个具有真实操作能力的 Agent 都拥有可检查、可控制、可恢复的执行历史，使用户能够放心地把重要任务交给 AI。

### 5.2 产品目标

1. 让用户在一分钟内看懂一次 Agent 运行发生了什么。
2. 让高风险操作在执行前进入统一审批流程。
3. 让失败任务能够从关键步骤稳定重放或分叉。
4. 让每个重要结果都能追溯到来源、工具输出或验证结果。
5. 让不同 Agent、模型和工具的运行能够在同一套界面中比较。

### 5.3 非目标

产品初期不负责：

- 自行训练基础模型；
- 替代 Codex、Claude Code 等 Agent 客户端；
- 成为通用聊天应用；
- 自动判断所有业务结果是否正确；
- 为恶意 Agent 提供不可突破的绝对安全保证；
- 在没有用户授权的情况下接管第三方账号或系统。

## 6. 产品定位

### 6.1 核心定位

AFR 是 Agent 与真实工具之间的“运行记录与控制层”，不是新的通用 Agent，也不是单独的 MCP 工具集合。

### 6.2 为什么不能只做 MCP Server

MCP 适合暴露标准化工具，但普通 MCP Server 只能看到经过它的调用。如果 Agent 可以直接访问终端、文件系统、浏览器或其他 MCP Server，单一 MCP 就无法获得完整记录，也无法提供可靠的全局审批和回放。

因此产品应采用：

- **完整程序作为核心**：负责事件存储、权限策略、快照、证据关系、回放和界面；
- **MCP 作为适配器**：提供记录查询、审批申请、证据提交和回放触发等能力；
- **SDK、Hook 或代理网关作为采集层**：捕获不经过 MCP 的模型与工具活动。

### 6.3 产品形态

第一阶段形态：

- 本地后台服务；
- 本地 Web 控制台；
- SQLite 数据库；
- 文件与命令操作代理；
- 一个 Agent 客户端适配器；
- 一个 MCP Server。

后续形态：

- 团队协作服务；
- 托管或私有部署；
- 多项目、多成员和审计策略；
- 跨设备的长期任务恢复。

## 7. 目标用户

### 7.1 独立开发者与高级 AI 用户

特征：频繁使用编码 Agent、终端和自动化工具，愿意尝试新工作流。

核心诉求：快速定位失败、保护本地项目、比较不同模型、复用成功运行。

### 7.2 Agent 应用开发团队

特征：正在开发客服、研究、运营、数据或编程 Agent。

核心诉求：调试 Agent、建立回归测试、观察工具调用、评估版本变化、降低线上事故率。

### 7.3 企业平台与安全团队

特征：需要让员工或内部 Agent 接入企业数据与业务系统。

核心诉求：统一权限、审计记录、敏感数据控制、事故追踪和合规导出。

### 7.4 Agent 产品的最终审核者

特征：不一定懂技术，但需要批准或验收 Agent 的工作。

核心诉求：用业务语言看到“它准备做什么、影响什么、依据什么”，并安全地批准或拒绝。

## 8. 用户任务（Jobs to Be Done）

1. 当 Agent 修改我的项目时，我希望知道每处修改的原因和验证结果，以便放心接受变更。
2. 当 Agent 即将执行不可逆操作时，我希望看到影响范围并进行审批，以避免事故。
3. 当 Agent 运行失败时，我希望定位首个异常步骤，而不是阅读完整聊天记录。
4. 当我调整模型、提示词或工具版本时，我希望使用同一输入重放，以判断改动是否真正改善效果。
5. 当 Agent 生成报告时，我希望每个重要结论都能跳转到原始证据。
6. 当多个 Agent 并行协作时，我希望看到各自的操作和交接关系。
7. 当任务需要跨天继续时，我希望从经过确认的检查点恢复，而不是重新解释全部背景。

## 9. 核心产品概念

| 概念 | 定义 |
|---|---|
| Run | 一次完整的 Agent 任务运行 |
| Actor | 执行动作的人、Agent、模型或子 Agent |
| Step | 运行中的一个逻辑步骤，例如规划、工具调用或人工审批 |
| Event | 不可变的底层事件，例如模型响应、命令执行、文件写入 |
| Artifact | 运行产生或使用的文件、报告、代码、图片等成果 |
| Evidence | 支持某个结论或状态的来源、测试结果、日志或引用 |
| Snapshot | 某个时间点的文件、配置、上下文和关键外部状态记录 |
| Policy | 决定某个动作允许、拒绝或需要审批的规则 |
| Approval | 用户或授权角色对风险动作作出的决定 |
| Replay | 使用原始或模拟输入重新执行既有步骤 |
| Fork | 从历史检查点创建新的运行分支，并修改模型、提示词或参数 |
| Checkpoint | 已保存且适合恢复的稳定节点 |

## 10. 典型使用场景

### 10.1 编码 Agent 修改项目

AFR 记录 Agent 阅读的文件、运行的命令、代码 diff、测试结果和最终说明。用户可从时间线跳转到任意变更，并查看它由哪段指令和哪次工具调用触发。

### 10.2 高风险命令审批

Agent 准备删除目录、推送分支或部署服务时，AFR 暂停执行并展示命令、目标、影响范围、Agent 给出的理由和可恢复性。用户批准后继续，拒绝后将原因反馈给 Agent。

### 10.3 调试失败的研究 Agent

研究报告引用了错误结论。用户从结论跳转到证据，发现某网页抓取失败后 Agent 使用了无关摘要。用户从该步骤分叉，更换检索工具后重放后续流程。

### 10.4 对比模型或提示词

开发者选择同一个检查点，分别使用不同模型或提示词重放。系统比较成功率、成本、耗时、工具调用数、审批次数和结果差异。

### 10.5 多 Agent 协作审计

规划 Agent、编码 Agent 和测试 Agent 并行工作。AFR 以泳道方式显示各自步骤、共享文件冲突、任务交接和最终合并决策。

## 11. 核心用户流程

### 11.1 首次接入

1. 用户安装 AFR 本地程序。
2. 选择要接入的 Agent 客户端。
3. 安装对应适配器或配置 MCP。
4. 选择项目目录和默认权限模板。
5. 运行测试任务。
6. 控制台显示第一条完整运行记录。

首次接入目标：技术用户在 10 分钟内完成；无需理解内部事件模型。

### 11.2 正常记录

1. Agent 开始任务，AFR 创建 Run。
2. 适配器持续发送模型、工具和文件事件。
3. AFR 对事件进行关联、去敏和持久化。
4. 控制台实时显示阶段、耗时和变更。
5. 任务结束后生成成果、证据与风险摘要。

### 11.3 审批流程

1. Agent 请求执行动作。
2. 策略引擎判断为允许、拒绝或需要审批。
3. 若需审批，执行暂停。
4. 界面展示动作、参数、目标、影响、理由和建议。
5. 用户选择仅本次允许、在限定范围内允许或拒绝。
6. AFR 记录审批者、时间、规则和结果。
7. 执行结果返回 Agent。

### 11.4 故障定位

1. 用户打开失败 Run。
2. 系统优先标记首个异常事件，而不是只标记最后报错。
3. 用户查看其上下文、输入、输出和关联变更。
4. 系统给出“模型判断错误、工具失败、权限拒绝、外部状态变化、验证未通过”等分类。
5. 用户从异常前检查点发起重放或分叉。

### 11.5 回放与分叉

1. 用户选择历史 Step 或 Checkpoint。
2. 选择原样重放、使用模拟工具返回或重新访问外部系统。
3. 可修改模型、提示词、策略和环境变量。
4. AFR 在隔离工作区中执行。
5. 系统生成新旧运行差异报告。

## 12. 功能需求

### 12.1 P0：MVP 必须具备

#### 运行采集

- 创建、结束和标记 Run；
- 记录模型请求与响应的必要元数据；
- 记录工具名称、参数、结果、状态和耗时；
- 记录终端命令、退出码和标准输出摘要；
- 记录文件读取、创建、修改、删除及 diff；
- 记录人工审批事件；
- 支持事件实时写入和异常恢复。

#### 时间线与检查

- 按时间展示 Run 中的所有 Step；
- 按 Actor、事件类型、风险等级筛选；
- 展开查看原始输入输出；
- 从文件事件跳转到 diff；
- 明确标记失败、重试、取消和未完成状态；
- 自动生成运行摘要。

#### 权限与审批

- 内置允许、拒绝、审批三态规则；
- 支持按工具、命令、路径和动作类型设置规则；
- 默认拦截文件删除、外部发送、部署和支付类动作；
- 审批界面必须展示具体目标和影响范围；
- 记录完整审批日志；
- 不允许 Agent 自行修改保护自身的策略。

#### 文件快照与恢复

- 在高风险文件操作前创建快照；
- 保存变更前后 diff；
- 支持从检查点复制到隔离工作区；
- MVP 不直接覆盖用户当前工作区进行回放。

#### 基础回放

- 从指定工具调用前重新执行；
- 支持复用已记录的工具返回进行离线重放；
- 支持修改模型或提示词后分叉；
- 显示新旧分支的关键差异。

#### 接入能力

- 首发支持一种主流编码 Agent；
- 提供 MCP Server；
- 提供命令与文件操作代理；
- 提供最小 Python 或 TypeScript SDK；
- 支持导入、导出单次 Run。

### 12.2 P1：产品可用性增强

- 结论与证据的可视化关联；
- 自动测试和验证步骤识别；
- Token、模型费用和工具成本统计；
- 多模型并排对比；
- 可复用的权限模板；
- 敏感数据识别与遮盖；
- 多 Agent 泳道视图；
- Webhook 和 CI 接入；
- 失败原因自动聚类；
- Run 分享包和只读审计报告。

### 12.3 P2：团队与企业能力

- 团队工作区、角色与审批链；
- 集中式策略管理；
- SSO、审计导出和保留周期；
- 私有部署和对象存储；
- 跨设备长期任务；
- Agent 质量基线与回归测试集；
- 风险趋势看板；
- 与工单、代码托管和安全平台集成。

## 13. 信息架构与主要界面

### 13.1 首页 / Runs

显示近期运行、状态、Agent、项目、耗时、成本、风险事件和结果。支持按失败、待审批、高风险和未验证筛选。

### 13.2 Run 详情页

建议采用三栏布局：

- 左侧：时间线和 Actor 泳道；
- 中间：当前步骤详情、模型输入输出和工具结果；
- 右侧：文件 diff、证据、风险和关联成果。

### 13.3 审批中心

集中展示待审批动作。每条审批必须回答：

- 谁要执行？
- 准备做什么？
- 作用于哪个目标？
- 为什么需要做？
- 可能造成什么影响？
- 是否可恢复？
- 批准范围是一次、当前 Run 还是指定规则？

### 13.4 Replay Lab

选择检查点、模型、提示词、工具模式和环境，发起新分支；完成后展示结果、成本、耗时、文件和验证差异。

### 13.5 Policies

管理权限规则、风险分级、敏感路径、允许域名、外部发送目标和审批人。

### 13.6 Artifacts & Evidence

集中查看一次运行产生的成果，以及每项成果关联的来源、测试和生成步骤。

## 14. 风险等级建议

| 等级 | 示例 | 默认处理 |
|---|---|---|
| R0 只读 | 读取普通项目文件、查询公开资料 | 允许并记录 |
| R1 可恢复写入 | 在隔离分支修改代码、生成临时文件 | 允许、快照并记录 |
| R2 有限副作用 | 安装依赖、调用有费用 API、创建云端草稿 | 根据策略允许或审批 |
| R3 外部影响 | 发送消息、推送代码、部署、修改权限 | 必须审批 |
| R4 高风险或不可逆 | 删除重要数据、付款、公开发布、处理敏感数据 | 强制审批并提供额外确认；部分操作要求用户亲自完成 |

风险等级不仅由工具名称决定，还应考虑参数、目标、环境和可恢复性。例如，`rm` 删除临时目录与删除项目根目录不能视为同一风险。

## 15. 产品架构

```text
Agent 客户端 / Agent 框架 / 多 Agent 系统
                 │
       SDK · Hook · Gateway · MCP Adapter
                 │
          Event Collector / Normalizer
                 │
     ┌───────────┼────────────┐
     │           │            │
 Policy Engine  Evidence     Snapshot Manager
     │          Graph           │
     └───────────┼────────────┘
                 │
        Event Store + Artifact Store
                 │
       Web Console · Replay Worker · Exporter
```

### 15.1 关键设计原则

- **事件不可变**：原始事件只能追加，修正信息以新事件表示；
- **本地优先**：默认数据不离开用户设备；
- **最小权限**：适配器只获得完成记录所需权限；
- **可降级**：某类事件无法采集时明确标记缺口，不能伪装成完整记录；
- **可移植**：Run 可导出为开放格式；
- **可验证**：摘要必须能够回到原始事件；
- **安全回放**：默认在隔离环境中回放，不重复真实外部副作用。

## 16. 事件数据模型草案

```json
{
  "event_id": "evt_01...",
  "run_id": "run_01...",
  "parent_event_id": "evt_00...",
  "timestamp": "2026-09-03T10:00:00+08:00",
  "actor": {
    "type": "agent",
    "id": "coding-agent-1",
    "model": "provider/model-version"
  },
  "event_type": "tool.call",
  "tool": {
    "name": "shell.exec",
    "arguments": {
      "command": "npm test"
    }
  },
  "risk": {
    "level": "R1",
    "policy_id": "policy_default_shell",
    "decision": "allow"
  },
  "result": {
    "status": "success",
    "duration_ms": 8421,
    "output_ref": "blob_sha256..."
  },
  "evidence_refs": ["evidence_01..."],
  "snapshot_before": "snapshot_01...",
  "snapshot_after": "snapshot_02..."
}
```

原始内容、二进制文件和较大日志应存入内容寻址的 Blob/Artifact Store，事件只保存引用、哈希和必要摘要。

## 17. MCP 接口草案

MCP 是接入层，不是唯一采集路径。初期可提供：

| Tool | 用途 |
|---|---|
| `start_run` | 创建运行并声明任务、项目和 Agent |
| `record_event` | 写入自定义 Agent 或工具事件 |
| `attach_evidence` | 将来源、测试或文件关联到结论和步骤 |
| `request_approval` | 为具体高风险动作申请审批 |
| `create_checkpoint` | 创建可恢复检查点 |
| `get_run_context` | 查询当前 Run 状态和已确认事实 |
| `search_history` | 检索历史运行及经验 |
| `finish_run` | 结束运行并提交结果状态 |
| `replay_run` | 请求创建回放或分支任务 |

所有工具必须验证调用者身份和 Run 范围。`request_approval` 的返回结果不能由 Agent 伪造，审批应由 AFR 核心程序签发。

## 18. 隐私与安全要求

### 18.1 默认隐私策略

- 所有记录默认保存在本地；
- 默认不上传提示词、文件内容和工具输出；
- 用户可配置不记录的目录、字段和工具；
- 密钥、Cookie、令牌和常见个人信息在持久化前遮盖；
- 导出前再次运行敏感信息检查。

### 18.2 完整性

- 事件使用哈希链或签名防止静默篡改；
- 明确记录日志暂停、采集失败和事件缺口；
- Agent 不能删除或改写自己的审批历史；
- 高风险动作的真实参数必须在审批后保持一致，参数变化需要重新审批。

### 18.3 回放安全

- 外部发送、付款、删除和生产修改默认使用模拟结果；
- 真实重放需要重新审批；
- 文件回放默认使用临时目录、容器或独立工作树；
- 明确标记模拟事件与真实事件。

## 19. 产品差异化

| 替代方案 | 能解决什么 | 仍然缺少什么 |
|---|---|---|
| 聊天记录 | 查看人机对话 | 工具级事件、快照、统一审批和稳定回放 |
| 普通日志平台 | 搜索技术日志 | Agent 语义、证据关系、文件 diff 和审批体验 |
| Git | 保存代码变化 | 模型输入、工具输出、外部动作和失败链路 |
| LLM 网关 | 统计模型请求、成本和延迟 | 文件、终端、浏览器和业务工具的完整行为 |
| 单一 MCP Server | 观察经过该 Server 的调用 | Agent 的其他工具和直接系统操作 |
| Agent 评测框架 | 离线测量任务表现 | 真实运行审计、人工审批和事故恢复 |

AFR 的核心差异应是：把“观察、控制、证据、恢复”做成一条完整链路，而不是只增加更多日志。

## 20. 商业模式假设

### 20.1 开源核心

建议将本地单机版和基础事件格式开源，降低 Agent 框架接入门槛，并建立生态信任。

### 20.2 付费能力

- 团队共享与多成员审批；
- 集中策略和审计管理；
- 私有化部署；
- 长期事件存储和高级搜索；
- 自动回归评测；
- 企业身份、合规与支持服务。

### 20.3 初步套餐假设

- Community：本地单用户、开源、核心记录与回放；
- Pro：个人多项目、高级对比、长期历史；
- Team：共享 Runs、审批流和团队策略；
- Enterprise：私有部署、SSO、审计与定制集成。

定价应在完成用户访谈和 MVP 使用验证后确定。

## 21. 成功指标

### 21.1 北极星指标

**每周被用户用于检查、审批或成功恢复的有效 Run 数。**

单纯记录大量事件不代表产品有价值；用户是否利用记录做出了判断或完成了恢复才是关键。

### 21.2 激活指标

- 安装后 10 分钟内完成首次接入；
- 首日成功记录至少一个包含工具调用和文件变更的 Run；
- 用户首次打开 Run 后能在一分钟内找到关键变更。

### 21.3 价值指标

- 故障平均定位时间下降；
- 高风险动作中被正确拦截的比例；
- 用户从检查点成功恢复的比例；
- 带测试或证据的完成结果占比；
- 用户主动查看 Run 详情和 diff 的频率；
- 团队对 Agent 自动化权限的提升幅度。

### 21.4 质量指标

- 事件丢失率；
- 采集造成的执行延迟；
- 敏感信息遮盖召回率；
- 回放结果可重复率；
- 错误审批和重复审批比例；
- Agent 客户端升级后的适配稳定性。

## 22. MVP 范围

### 22.1 MVP 目标

证明三个核心假设：

1. 用户愿意为了可检查的运行历史安装采集层；
2. 时间线、diff 和审批能明显提升对 Agent 的信任；
3. 从检查点分叉比完整重跑更有调试价值。

### 22.2 MVP 包含

- 支持一种编码 Agent；
- 本地后台服务和 Web 控制台；
- SQLite 事件存储；
- 命令与文件事件；
- Git diff 或文件快照；
- 三态权限策略；
- 待审批界面；
- 从工具调用前创建分支；
- 原始事件 JSON 导出；
- 最小 MCP 接口。

### 22.3 MVP 不包含

- 完整企业权限体系；
- 跨设备云同步；
- 所有 Agent 框架适配；
- 自动完成复杂业务正确性判断；
- 真实生产环境的无确认回放；
- 大规模分布式事件分析。

## 23. MVP 验收标准

1. Agent 完成一次代码修改后，用户能在控制台看到任务、命令、文件 diff 和测试结果。
2. Agent 请求删除受保护文件时，操作会暂停，未经批准不能继续。
3. 审批记录包含动作参数、审批者、时间和决定。
4. Agent 执行失败时，用户可以从失败前一步创建隔离分支。
5. 分支运行不会修改原始工作目录。
6. 用户能导出一份可离线查看的 Run 记录。
7. 关闭并重启 AFR 后，未完成 Run 和历史事件仍然存在。
8. 产品明确显示无法采集或缺失的事件范围。

## 24. 建议开发路线

### 阶段 0：技术验证，1～2 周

- 定义事件格式；
- 捕获一个 Agent 的命令和文件变更；
- 保存 SQLite；
- 用最简单的页面展示时间线；
- 验证适配器升级风险。

### 阶段 1：可演示 MVP，3～5 周

- 完成 Run 详情页；
- 加入 diff 和风险分类；
- 加入审批暂停与恢复；
- 支持隔离分支回放；
- 制作三个真实演示案例。

### 阶段 2：开发者预览，4～8 周

- 先交付 Codex CLI JSONL Adapter，再验证 Codex SDK / App Server；
- 让 AFR 托管 Codex 生命周期，默认在隔离 worktree 中执行，并通过审批与 Patch Promotion 控制源工作区副作用；
- 发布通用 Adapter 契约和最小 MCP；
- 在 Codex 真实接入稳定后增加 DeepSeek 等第二 Agent Adapter；
- 加入证据关联和成本统计；
- 招募 20～50 名高频 Agent 用户；
- 根据真实事故调整策略模型。

### 阶段 3：团队版验证

- 共享工作区；
- 审批角色；
- 集中策略；
- CI 与代码托管集成；
- 验证团队付费意愿。

## 25. 首批演示案例

### Demo A：拦截危险删除

Agent 误将清理构建目录扩大为项目目录。AFR 根据目标路径提升风险等级、阻止执行，并展示将受影响的文件数量。

### Demo B：代码修改的证据链

Agent 修复 Bug。用户可以从最终代码跳转到需求、相关文件、运行命令和通过的测试，并看到每处 diff 的来源。

### Demo C：失败后分叉

Agent 选择了错误依赖。用户从安装依赖前分叉，修改约束并重新运行，系统自动比较两条路径的耗时、文件和测试结果。

## 26. 主要风险与应对

### 26.1 无法获得完整事件

风险：不同 Agent 客户端暴露的 Hook 能力不同。

应对：明确“完整度等级”；结合 SDK、代理、文件监视和 MCP；不把缺失数据包装成完整审计。

### 26.2 记录过多导致界面不可用

风险：一次运行可能产生数千事件。

应对：原始事件与逻辑 Step 分层；默认折叠低价值事件；优先展示异常、风险、变更和验证。

### 26.3 日志本身包含敏感数据

风险：完整记录可能成为新的数据泄露面。

应对：本地优先、字段级遮盖、目录排除、加密存储、保留周期和导出检查。

### 26.4 回放产生重复副作用

风险：重复发送消息、付款或删除数据。

应对：外部副作用默认模拟；真实重放必须重新审批；为事件增加幂等键和副作用标签。

### 26.5 适配器维护成本过高

风险：Agent 客户端更新频繁。

应对：优先支持开放事件标准；首发只支持一种客户端；让社区维护非核心适配器。

### 26.6 用户把产品误认为绝对安全沙箱

风险：记录和审批不能消除所有安全问题。

应对：清晰定义安全边界；高风险环境结合容器、虚拟机和操作系统权限；界面显示实际覆盖范围。

## 27. 待验证假设与用户访谈问题

### 27.1 关键假设

- 用户最先付费的价值是调试、审批还是合规审计？
- 用户是否接受通过代理层运行命令和文件工具？
- 本地 Web 控制台是否足够，还是必须嵌入 Agent 客户端？
- 用户需要完整模型内容，还是只需要摘要和引用？
- 回放的核心价值来自模型对比、故障恢复还是回归评测？

### 27.2 访谈问题

1. 最近一次 Agent 造成的真实错误是什么？最终如何发现？
2. 哪些操作你从不允许 Agent 自动执行？
3. 验收 Agent 成果通常需要多长时间？
4. 出错后你会重新运行、人工修复还是放弃？
5. 哪些内容绝不能进入日志？
6. 你更想看到完整记录，还是系统标出的三个关键步骤？
7. 如果只能解决一个问题，你会选择审批、调试、证据还是回放？

## 28. 产品原则

1. **先让行为可见，再谈自动化程度。**
2. **重要动作必须明确目标与影响。**
3. **完成不等于正确，结果必须能够被验证。**
4. **记录不能依赖 Agent 自我汇报。**
5. **回放默认不重复真实副作用。**
6. **采集缺口必须诚实呈现。**
7. **用户永远保留最终控制权。**

## 29. 需要尽快做出的产品决策

1. 首发 Agent 已决定为 Codex；固定示例 Agent 继续用于确定性回归，DeepSeek 等第二供应商在 Codex Adapter 稳定后验证；
2. 首发操作系统是否只支持 macOS；
3. 使用 Git 工作树、容器还是临时目录作为默认回放环境；
4. 原始模型输入输出默认完整保存还是按需保存；
5. 开源范围和许可证；
6. 本地控制台采用桌面壳还是浏览器页面；
7. 产品正式名称与品牌方向。

## 30. 最终产品判断

AFR 不应被定义为一个“记录日志的 MCP”，而应被定义为 Agent 的运行控制与审计基础设施。MCP 能帮助它快速接入生态，但真正形成产品价值的是以下闭环：

> 捕获真实行为 → 判断风险 → 请求审批 → 保存证据与快照 → 定位失败 → 安全回放或分叉 → 比较结果。

只要这个闭环能够在一个真实 Agent 场景中稳定运行，产品就已经具备独立价值；之后再扩展更多 Agent 和企业能力，而不是一开始追求支持所有框架。

Codex 的完整接入不以“读取全部模型内部思维”为目标，而以“公开事件可记录、关键副作用可控制、实际结果可验证、未知缺口可见”为目标。具体 Hosted 模式、隔离、Gateway 和发布红线见 [`docs/codex-hosted-architecture.md`](./docs/codex-hosted-architecture.md)。
